{
  "name": "gemini-netlify-proxy",
  "version": "1.0.0",
  "description": "Proxy function for accessing the Gemini API securely.",
  "main": "functions/gemini-proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^0.1.3"
  }
}
